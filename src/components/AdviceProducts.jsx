import { useState } from "react";

const AdviceProducts = ({ product }) => {
    return (

        <div className="adv">
            <h2>{product.name}</h2>
        </div>
    )
}

export default AdviceProducts;