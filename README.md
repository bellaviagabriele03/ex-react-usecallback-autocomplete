# REACT + VITE

## esercizio di test su useCallback, funzione di debounce per creare una barra di ricerca che mostri dei suggerimenti in base alle ricerche effettuate, migliorando le performance di tutta l'intera app: per non effettuare una chiamata API ogni volta che un utente scrive nell'input ! 

### NOTA: migliorare parte grafica del componente "AdviceProducts" 